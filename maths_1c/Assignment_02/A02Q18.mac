log_base(x,y) := log(x)/log(y);

base: rand([2,3,5]);

n: rand([1,3]);
k: rand(3)+1;

/* Question is: evaluate the following expression: */
/* \log_{base^2}(base^n) - \log_{base}(base^(-k)) */
tans1: n/2 + k;
